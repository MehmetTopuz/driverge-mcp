#!/usr/bin/env node
// Driverge MCP server — entry point.
//
// TODO(Session 1): stand up the @modelcontextprotocol/sdk stdio server here
// with one dummy `ping` tool. This file is currently a bootstrap smoke stub so
// the toolchain (tsc -> dist/server.js, the `driverge` bin) is wired and
// verifiable end-to-end before any product code lands.

console.error("driverge-mcp: not yet implemented");
